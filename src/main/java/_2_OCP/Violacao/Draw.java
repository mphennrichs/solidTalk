package _2_OCP.Violacao;

public class Draw {

    public void drawMoney(Double value, AccountTypeEnum accountType){

        if (AccountTypeEnum.CHECKING.equals(accountType)){
            //draw from checking account
        }

        if (AccountTypeEnum.SAVINGS.equals(accountType)){
            //draw from checking account
        }
    }
}